List<String> arr = Files.readAllLines(Paths.get(""));

Files.readAllLines(Paths.get(""))
	.foreach(sout ...)
	
BufferedReader reader = new BufferedReader(new FileReader("ime na fail"));

reader.readLine ...